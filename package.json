{
  "name": "ego-react-scraper",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://marcusoftnet.github.io/ego-react-scraper/",
  "dependencies": {
    "axios": "^0.18.0",
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "prestart": "npm run lint",
    "start": "react-scripts start",
    "prebuild": "npm run lint",
    "build": "react-scripts build",
    "pretest": "npm run lint",
    "test": "react-scripts test",
    "lint": "standard --fix",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "standard": "^12.0.1"
  }
}
